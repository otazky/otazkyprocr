<%= form_for(@citizens_task, :remote=>true) do |f| %>

         <p>
    Přidejte podúkol:
    <%= text_area :subtask,:body, :rows=>2, :cols=>20 %>
            </p>
   <p> kolik času chcete úklu věnovat:

    <%= f.text_field :hours %> h
     </p>
  <div class="actions">
    <%= f.submit "Uložit", :class=>'btn btn-primary'%>
  </div>
<% end %>
